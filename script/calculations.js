const calculations = {
    sum: (x, y) => parseFloat(x) + parseFloat(y),
    subtract: (x, y) => parseFloat(x) - parseFloat(y),
    multiply: (x, y) => parseFloat(x) * parseFloat(y),
    divide: (x, y) => parseFloat(x) / parseFloat(y),
    percent: (x) => parseFloat(x) / 100,
}